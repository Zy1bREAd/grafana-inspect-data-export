# 已完成目标（用于压缩存储上下文信息）
请求Grafana API获取Grafana中MySQL慢查询日志的JSON响应数据，将其转换并生成Excel文件，并通过Gitlab API请求将文件上传至特定Issue中。

--- 

# 新目标
新增命令选项参数，增强构建二进制命令执行时的健壮性、完整性。

## 要求
1. 基于flag包实现
2. 使用长选项参数，例如 `--config` 表示配置文件位置
